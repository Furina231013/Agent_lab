# Embedding Demo

Loader reads local files from disk and turns them into plain text.

Chunker splits long text into smaller chunks so retrieval stays manageable.

Vector search compares embeddings instead of only comparing exact keywords.
